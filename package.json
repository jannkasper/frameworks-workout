{
  "name": "my-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "cd react-components && npm install",
    "start": "cd react-components && npm start",
    "build": "cd react-components && npm build"
  }
}
